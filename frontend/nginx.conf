# Песочница — статика на 8081 (порт закреплён решением проджекта, #12).
server {
    listen 8081;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }
}
