# Журнал координатора

Ведётся сессией-координатором по [регламенту](../docs/roles/karina.md).
Запись на каждое решение о выдаче задачи и на каждое закрытие: дословное
задание сэра, кому выдано и почему именно так, что вернулось в рапорте,
что помешало. Журнал уезжает в `main` одним pull request на смену вместе
с `REPORT.md`, а не по одному на задачу.

Форма записи:

```
## <дата> · #<номер тикета> · <Имя>-<номер тикета>

**Задание сэра (дословно):** …
**Тикет:** #N — <заголовок>
**Копия и ветка:** .worktrees/<slug>, <type>/<slug>
**Условие старта:** нет / <какое, кто следит>
**Рапорт:** <кратко: PR, CI, находки ревью, проблемы>
**Проверка факта:** gh pr view <N> → merged, gh pr checks → зелёный
**Закрыт:** <когда, с каким комментарием>
**Что помешало:** <отказы механизмов, задержки, дубли — или «ничего»>
```

---

## 2026-08-27 · #65 · закрыт без исполнителя

**Задание сэра (дословно):** «закрой #65 и бери #103»
**Проверка факта:** `gh pr view 145` → MERGED, слияние `cbd98aa`;
`grep -c newest_source_mtime .claude/hooks/gate-quality.py` → 0.
**Закрыт** комментарием с оговоркой о происхождении: работа сделана попутно,
при переходе на схему ролей, а не сессией-исполнителем по этому тикету.
Проверка с моей стороны машинная — факт слияния и прогон набора, не ревью.
**Что помешало:** ничего.

## 2026-08-27 · #103 · Рустем-103

**Задание сэра (дословно):** «закрой #65 и бери #103»
**Тикет:** #103 — [обвязка] разбор не различает природу строки: путь,
встроенный код, чужая ФС, литерал
**Копия и ветка:** `.worktrees/parse-by-nature-of-string`,
`fix/parse-by-nature-of-string`
**Условие старта:** было в теле задачи — «не браться, пока не слиты #97 и #53».
Проверено до выдачи: #97 закрыт, PR #53 слит. Условие наступило.
**Что добавлено в тикет перед выдачей:** свежие цифры из `guard.jsonl`
(19 отказов 26 августа, ложных около семнадцати; 59 односегментных «путей»
за три дня: `/gm` ×19, `/feature` ×3, `/karina`, `/60`, `/M`, `/слово:`),
два направления сужения как гипотезы, критерии приёмки и поправки к телу
задачи под новую схему — журнал, приёмка, слияние.
**В задании отдельно:** правка `.claude/hooks/` требует пропуска
`protected-files`; просить через координатора, не обходить.
**Рапорт:** ожидается.
**Закрыт:** —
**Что помешало:** `scripts/unlock.sh --list` координатору недоступен — хук
отклоняет запуск скрипта из сессии агента целиком, не различая выдачу
и просмотр. В регламенте этот пункт неверен, правка пойдёт журнальным PR.

### Дефект схемы, найденный исполнителем в первые минуты #103

Адрес координатора, подставленный в задание («2-4e»), устарел через несколько
минут после запуска: автоимя сессии сменилось на «Карина». Исполнитель
не смог отправить рапорт по указанному адресу, нашёл координатора сам
в `ListAgents` и **сказал об этом вместо того, чтобы молча подстроиться**.

Цена, если бы не сказал: следующее задание ушло бы с тем же неверным адресом,
а сессия, которая не нашла бы координатора в списке, встала бы молча — тихая
остановка, неотличимая снаружи от медленной работы.

**Правка в регламент (в конце смены):** в задание подставляется имя вместе
с устойчивой ссылкой — `Карина [2a8ba0]`. Ref за смену не менялся, имя
менялось. Исполнителю — правило: имя не резолвится, пиши по ref.
