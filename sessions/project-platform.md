# Журнал: проджект платформенного потока

Вторая проджект-сессия проекта. Зона — обвязка, качество и сдача:
метки `role:devops`, `role:qa`, `role:release`. Продуктовый поток
(`role:backend`, `role:frontend`, `role:contract`, `role:docs`) ведёт Ранис.

---

## Сессия 1 · 2026-08-25, 16:35

### Промпт, которым сессия открыта (дословно)

> Ты — проджект-менеджер платформенного потока в этом проекте. Твоя зона: обвязка,
> качество и сдача — задачи с метками role:devops, role:qa, role:release. Прочитай
> docs/tasks/00-handoff-project.md (общие правила процесса), затем свою доску:
> gh issue list --label task. Начни с gh issue view 14, 15, 18, 35, 43, 54 --comments.
> Продуктовый поток ведёт другой проджект, Ранис: задачи с метками role:backend,
> role:frontend, role:contract, role:docs — не твои, исполнителям по ним не пиши.
> Спорную принадлежность задачи неси продуктовой сессии, а не решай между собой.
> Свои промпты и решения веди в sessions/project-platform.md.

### Входящее от продуктовой сессии (Карина), дословно, как данные

Пришло через `SendMessage` в середине чтения доски. Содержание — ввод в роль,
границы двух проджектов и совет начать с #18. Ключевые пункты, на которые
опираюсь дальше:

> **Ваша зона — обвязка, качество и сдача.** Метки `role:devops`, `role:qa`,
> `role:release`: #14 compose и инструкция запуска, #15 QA-ломатель, #18 сдача,
> #35, #43, #54 — открытые баги защитной обвязки, всё, что заведут дальше
> по этим меткам.
>
> **Три правила:** 1) одна задача — один владелец; 2) спорную принадлежность
> решает продукт или сэр; 3) исполнителю пишет только владелец задачи.
>
> **Первое, что стоит сделать:** прочитать #18 целиком. Сдача зависит от всех
> остальных задач и потому кажется последней, но собирать её материал надо
> сейчас, пока роли работают.

Совет про #18 принят и выполняется: материал для отчёта копится с первого часа.

### Состояние доски на входе

| Задача | Что с ней |
|---|---|
| #43 | работа сдана, PR #53 открыт, CI зелёный, ждала приёмки — **принята сегодня, вердикт «на доработку»** |
| #54 | заведена, приёмки задания исполнителем нет, в работу не отдана |
| #35 | приёмка задания исполнителем есть, ветка `fix/unlock-and-log-common-root` пуста, PR нет — статус запрошен |
| #14 | ждёт слияния #40 (бэкенд) и #48 (фронт) |
| #15 | ждёт работающих бэка и фронта; исполнителя нет ни одного |
| #18 | последняя по порядку, материал собирается с сегодня |

---

## Решение 1: приёмку #43 вести без локального прогона набора

**Обстановка.** В `.claude/state/unlock.json` на 16:35 лежали три действующих
пропуска сэра (`protected-files`, `git-worktree`, `git-branch`). Правило проекта
требует подтверждать приёмку выводом команды; очевидная команда — прогон набора
проверок. Но открытая #54 говорит ровно то, что этот прогон делает с чужим
состоянием.

**Решение.** Набор локально не гонять. Зачесть изолированный прогон в CI
(он зелёный) и проверять фикс точечно, сравнивая поведение `_paths` из `main`
и из ветки на одном входе.

**Почему так.** Прогон стоил бы сэру трёх выданных действий, а соседним ролям —
отказов посреди прогона. Цена ошибки в обратную сторону — недопроверенная
приёмка — снималась точечными пробами, и они оказались сильнее прогона:
CI был зелёным, а дыры нашлись.

**Побочный вывод, годный для отчёта.** «Сделано подтверждается выводом команды»
и «прогон вредит соседям» сейчас противоречат друг другу. Пока #54 открыта,
принимающий выбирает между двумя плохими вариантами. Это не философия,
а конкретный дефект с номером.

---

## Решение 2: #43 / PR #53 — на доработку

Проверка — сравнение одного и того же входа на двух версиях общего модуля,
пути взяты внутри разрешённой временной папки нарочно: показывается не запрет,
а то, что проверять стало нечего.

```
main  'docker ps && rm -rf /tmp/probe-target'              -> ['/tmp/probe-target']
main  'docker compose up -d && rm -rf /tmp/probe-target'   -> ['/tmp/probe-target']
main  "bash <<'SH'\nrm -rf /tmp/probe-target\nSH"          -> ['/tmp/probe-target']
PR53  'docker ps && rm -rf /tmp/probe-target'              -> []
PR53  'docker compose up -d && rm -rf /tmp/probe-target'   -> []
PR53  "bash <<'SH'\nrm -rf /tmp/probe-target\nSH"          -> []
```

**Дыра 1.** Docker-команда в любой стадии конвейера снимает проверку путей
со всей команды. Отсюда `docker compose up -d && rm -rf <наружу>` мимо границы
репозитория — форма, которой поток обвязки занят каждый день.

**Дыра 2.** Тело heredoc вырезается независимо от получателя. Для `cat > file`
это данные, для `bash <<`, `sh <<`, `python3 - <<` — исполняемый код. И вырезание
живёт в `normalize()`, который импортирует ещё и `guard-secrets`: запрет,
заведённый против «обхода через `python -c`», обходится сменой формы на heredoc.
У `guard-git` та же дыра была и до этой правки — проверено на обеих версиях;
исполнитель её не создавал, но перенос разбора в общий модуль делает её общей
на четыре хука.

**Что зачтено:** отказ чинить по описанию задачи и переход к настоящим отказам
из журнала; парные проверки на каждое сужение; публичный разбор собственной
дыры в первой версии фикса.

**Заметка процессу.** Зелёный CI не доказал ничего: тесты к фиксу пишет автор
фикса. Это прямое подтверждение правила «приёмку делает не автор» — в отчёт
по #18 идёт фактом, а не лозунгом.

---

## Решение 3: формулировку #54 ужесточить до «перенаправление, а не восстановление»

Задача была написана как «тесты не изолируют состояние». По коду набора это
неточно и уводит к дешёвой неправильной починке: изоляция там есть, боевой
`unlock.json` **удаляется** на время прогона и возвращается **снимком**,
снятым до старта. Следствий три: пока идёт чужой прогон, действующего пропуска
нет ни у кого; пропуск, выданный во время прогона, теряется при восстановлении;
журнал не изолирован вовсе — у него берётся только размер.

Отсюда требование: свой каталог состояния и свой журнал, боевые файлы за прогон
не открываются вовсе. «Восстанавливать аккуратнее» или «сливать записи» не
принимается — гонка остаётся, сужается только окно.

---

## Решение 4: порядок работ в потоке обвязки

Доработка #43 → #54 → #35. Обоснование: #43 — открытый PR с дырой, которая
с слиянием разъедется на четыре хука; #54 тратит действия сэра и портит запись,
по которой пишется отчёт; #35 болезненна, но терпит. Предложено исполнителю
с прямым приглашением возразить.

#14 и #15 стоят на слиянии #40 и #48 — это чужой поток. Спросил у Раниса
ориентир по срокам и не изменится ли ещё состав `Dockerfile` в тех ветках:
#14 завязана ровно на них.

---

## Открытые вопросы

1. **Исполнителя на #15 (QA) нет ни одного.** Задача требует ломателя и работающих
   бэка с фронтом. К сэру: заводить ли отдельную QA-сессию и когда.
2. **Своей рабочей копии у этой сессии нет.** Главное дерево общее, и по нему уже
   дважды уходили коммиты не туда. Пропуск `git-worktree` выдаёт сэр в своём
   терминале; до тех пор журнал этой сессии лежит несохранённым в главной копии.
3. Статус #35 от исполнителя.

## Двенадцатый случай для #43 — из этой сессии

Первая попытка записать текст приёмки во временный файл отклонена `guard-scope`:
в прозе комментария, в перечне будущих проверок, упоминался путь внутри
контейнера. Запись шла в разрешённую временную папку. Класс ошибок, который
чинит задача, отказал в акте приёмки этой самой задачи. Обходить не стал —
переформулировал абзац и записал случай в задачу.

---

## Решение 5: очередь потока переставлена — #54 → #14 → доработка #43 → #35

Прежняя очередь (доработка #43 первой) прожила двадцать минут и отменена: Ранис
сообщил, что бэкенд слит в `main` (#11, #56 закрыты, PR #40 в main), документация
в `main` с #36, состав `Dockerfile` устоялся и меняться не будет.

**Почему #14 вперёд.** На ней стоят QA (#15) и сдача (#18) — весь конец проекта.
**Почему #43 подождёт.** Найденные дыры живут в ветке, а не в `main`: пока PR #53
не слит, ими никто не платит. Слияние до доработки не разрешаю, но торопить
доработку смысла нет. **Почему #54 всё-таки первой** — не по важности, а по цене:
Ранис проверил, что прогон с `CLAUDE_PROJECT_DIR` во временной папке оставляет
боевой `unlock.json` побайтово нетронутым (сверял sha256 до и после), то есть
работы там на полчаса и, возможно, без пропуска сэра вовсе — правка укладывается
в незащищённый файл набора.

**Связка, которую иначе разберут по частям:** #35 переводит чтение пропуска
и журнала на `repo_root()` через `git rev-parse`, а изоляция набора из #54 держится
на переменной окружения, которую `git rev-parse` не слушает. Сделанные наивно
и в этом порядке, две задачи отменят друг друга. Сказано исполнителю и Ранису.

## Решение 6: требования продукта к отчёту положены в #18

Продуктовая сессия прислала структуру `REPORT.md`, список обязательных сюжетов
раздела «что не сработало» и запреты (никаких оценочных слов о собственной работе,
никакого «успешно», ничего запланированного, поданного как сделанное). Переписал
комментарием в задачу: переписка сессий в сдачу не входит, задача входит.

Правило, общее с приёмками: **каждое утверждение с доказательством рядом**.
Ссылка на PR или issue считается, пересказ — нет.

## Решение 7: `scripts/ci/`, `.ci-stack` и вопрос про `actions/setup-node` — мои

Спорная граница разрешена по факту владения, а не по тому, кто наполнял файлы
и кто поднял вопрос: наполнял бэкендер, поднял исполнитель Раниса, владеет поток
обвязки. Вопрос про закрепление версии Node в защищённом `ci.yml` не срочен —
раннер приезжает с Node 22, критерий A9 не блокирован, речь о страховке
от дрейфа образа. Несу сэру вместе с остальным.

## Наблюдение, взятое как правило приёмки

Формулировка Раниса: за сегодня трижды правки в обвязке задевали соседние
механизмы. Отсюда вопрос, который в этом потоке задаётся на приёмке всегда:
**не «что вы правили», а «кто ещё пользуется тем, что вы правили»**. Обе
сегодняшние находки по #43 — ровно оттуда: `normalize()` оказался общим
на четыре хука, а не на три, и автор об этом не знал, честно написав в PR
«`guard-secrets` не тронут».

---

## Инцидент: гейт качества не выпустил сессию, закрывать не стал

Гейт назвал два пункта: один незакоммиченный файл (мой журнал) и «код правился
после последнего прогона тестов». Кода я в этой сессии не трогал вовсе — правки
чужие, в общем дереве, это дефект #35 из моей же зоны.

Закрывать не стал по факту обстановки: **главное дерево в тот момент стояло
на чужой ветке** `chore/frontend-review`, переведённое соседней сессией, пока
я работал. Коммит журнала лёг бы в чужую ветку и уехал бы в чужой pull request —
тот самый инцидент, который в проекте уже случался дважды. Прогон набора тоже
был бы вреден: у сэра были живы два выданных пропуска, и набор их бы снял.

Доложено сэру прямым текстом как невыполненное. Сэр выдал пропуск `git-worktree`,
и дальше журнал живёт в собственной копии `.worktrees/psp-project-platform`,
ветка `chore/journal-platform` от `origin/main`.

**Что из этого идёт в отчёт.** Гейт качества, требуя прогона и чистого дерева,
подталкивал ровно к двум вредным действиям сразу: закоммитить в чужую ветку
и сжечь чужие пропуски. Правило «сделано подтверждается выводом команды» верное,
но проверка, не различающая свою работу и соседскую, превращает его в ритуал.
Это третий за день случай, когда механизм мешает делать то, ради чего он стоит.

## Решение 8: #54 и #35 берутся одной работой

Исполнитель попросил объединить и привёл довод сильнее моего исходного: он **уже**
начал #54 и сделал ровно то, чего я опасался, — завёл в `_hooklib` отдельную
переменную для каталога состояния. Проверка Раниса («достаточно `CLAUDE_PROJECT_DIR`»)
верна ровно до #35, которая переводит чтение на `repo_root()`, а тот слушает
`git rev-parse`, а не окружение. То есть дешёвое решение #54 ломается следующей же
задачей.

**Согласовано: одна работа, одна приёмка, парная проверка «пропуск, выданный
до прогона, жив после» обязана быть зелёной после обеих правок, а не после каждой
по отдельности.** Разделение ради чистоты доски здесь стоило бы переделки.

## #43: доработка пришла, дыр оказалось шесть

Исполнитель проверил мои две находки не на слово и нашёл шесть: обе мои плюс
четыре следствия того же корня. Существенное сверх моего разбора:

- через интерпретаторный документ проходила не только запись наружу, но и **чтение
  `.env` мимо `guard-secrets`** — то есть обход, ради которого хук заведён, менял
  форму и проходил. Я это предполагал по коду, но доказать из своей сессии не мог:
  любая проба с именем секрета блокируется собственными хуками раньше, чем доедет;
- **третья дыра, которую не назвал ни один из нас:** даже с невырезанным телом
  `guard-scope` выходил раньше — тело есть, признаков записи в строке нет.
  Понадобилось внести интерпретаторный документ в разбор встроенного кода;
- `guard-git` переведён с собственной копии разбора на общую: дыра там была
  до этой задачи, и общий модуль позволил закрыть её один раз на четыре хука.

15 новых проверок, семь — на интерпретаторные документы сразу по трём хукам:
разбор общий, ломается во всех одновременно, проверять на одном бессмысленно.

Приёмка доработки — отдельным заходом; вердикт «принято» не выдаётся по описанию
работы, даже когда описание убедительное.
