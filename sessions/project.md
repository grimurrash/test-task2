# Журнал · роль «проджект-менеджер»

Сессия 2 · 25 августа 2026 · Claude Code, модель Fable 5, output style JARVIS.
Роль: проджект-менеджер. Что произвела сессия: доска из семи задач в GitHub Issues
(#9–#15) с зависимостями, порядком и правилами приёмки.

---

## Промпты сэра, дословно

1. `Ты — проджект-менеджер этого проекта. Прочитай docs/tasks/00-handoff-project.md
   и работай по нему. Свои промпты и решения веди в sessions/project.md.`

## Что произведено

Задачи по ролям — по issue на роль, в порядке зависимостей, телом по структуре
шаблона `.github/ISSUE_TEMPLATE/role-task.yml`:

| Issue | Роль | Ветка | Зависит от |
|---|---|---|---|
| [#9](https://github.com/grimurrash/test-task2/issues/9) | контракт `openapi.yaml` | `feature/contract-openapi` | PRD |
| [#10](https://github.com/grimurrash/test-task2/issues/10) | дизайн: макет песочницы и доков | `feature/design-sandbox` | PRD, параллельно с #9 |
| [#11](https://github.com/grimurrash/test-task2/issues/11) | бэкенд: API и тесты | `feature/backend-api`, worktree `.worktrees/psp-backend` | #9 |
| [#12](https://github.com/grimurrash/test-task2/issues/12) | фронт: песочница | `feature/frontend-sandbox`, worktree `.worktrees/psp-frontend` | #9, #10, #11 |
| [#13](https://github.com/grimurrash/test-task2/issues/13) | документация: страница Scalar | `feature/docs-openapi`, worktree `.worktrees/psp-docs` | #9 (curl-проверка — после #11) |
| [#14](https://github.com/grimurrash/test-task2/issues/14) | devops: compose и README | `feature/devops-compose` | #11, #12, #13 |
| [#15](https://github.com/grimurrash/test-task2/issues/15) | QA-ломатель | `feature/qa-breaker` | #14 |
| [#18](https://github.com/grimurrash/test-task2/issues/18) | сдача: README, REPORT, PDF, гигиена | `chore/release` | #9–#15, последняя |

Доска — `gh issue list --label task`. Контракт первым: три роли не видят контекста
друг друга, спор решается файлом.

## Решения проджекта

| Решение | Обоснование |
|---|---|
| Порты: API 8080, песочница 8081, документация 8082; переопределение через env | Кросс-ролевая константа; без неё compose (#14) собирал бы три сервиса с тремя мнениями о портах |
| CORS открыт на API; адрес API у фронта — `VITE_API_BASE` | Песочница живёт на другом origin; альтернатива (прокси в контейнере фронта) добавляет движущуюся часть без выгоды для песочницы |
| «Оканчивается на 01/02» формализуется как `amount_minor % 100` | Иначе суммы `1` и `2` бэк и фронт растолкуют по-разному; это уточнение формулировки продукта, не новое правило |
| Бэкенд и фронт входят в работу в два шага: сначала ревью контракта (и макета — для фронта), потом реализация | Требование перекрёстного ревью «до начала реализации» иначе невыполнимо: сессии этих ролей ещё не существуют, когда контракт готов. Сигнал на ревью даёт проджект |
| QA стартует после #14, а не после «бэк+фронт» | Ломать надо систему, поднятую как у проверяющего — `docker compose up`, а не dev-сборку |
| В задачах бэка/фронта/доков — примечание о пропуске `git-worktree` | `git worktree add` заблокирован хуком; пропуск выдаёт сэр, сессия сама не может — без примечания каждая роль упрётся в отказ |
| `scripts/acceptance.sh` и CI-скрипты — в задаче бэкенда | Это curl-сценарии против API и его тесты; devops их прогоняет на чистом клоне, но не пишет |

## Разрешение сэра о журнальных PR

25 августа: `мр смержи, в будущем можешь журналы сразу мержить`.

PR, где дифф — только `sessions/*.md`, сливаются сразу после создания, без
ожидания сэра: журнал описывает уже сделанное, ревьюить в нём нечего, а очередь
открытых PR отвлекает. Разрешение **не распространяется** на диффы с кодом,
контрактом, макетом и документацией продукта — эти по-прежнему ждут сэра, и приёмку
в них делает не автор.

Попутно: правило не удалось записать в постоянную память Claude Code — она лежит
за пределами репозитория, и `guard-scope` отказал в записи. Отказ правильный,
обходить его не стал; место такому правилу и так здесь, в журнале, который едет
вместе с репозиторием.

## Переписка с продуктовой сессией

Канал `SendMessage`, сессия `2-65`. Мои вопросы (не додумывать молча — правило
handoff): поведение без `X-Merchant-Id`; как платёж достигает `succeeded`/`failed`
для F15.

Ответы продукта, внесённые в задачи (в PRD — F7a, F17, F18, U4a, A12, A13):

1. `X-Merchant-Id` обязателен: отсутствует → 400 `merchant_id_required`;
   пустой/длинный/мусорный → 400 `invalid_merchant_id`. Песочнице — переключатель
   двух демо-мерчантов.
2. Терминальные статусы — правило тестовых сумм: копейки `01` → `failed`,
   `02` → `succeeded`, прочее → `pending`; поле `status_reason`. Песочнице —
   пресет «отменить завершённый платёж».

Вторым письмом продукт передал решение сэра о процессе, внесено во все семь задач:

- **Приёмка задания исполнителем** до первого коммита — комментарий из пяти пунктов
  (понял · не хватает · не согласен · объём и риски · вердикт); «нужны ответы»
  блокирует старт; полчаса на разбор.
- **Перекрёстное ревью** только там, где ошибка дороже проверки: контракт ревьюят
  бэкенд и фронт до приёмки; макет ревьюит фронт; Docker и документация — без него.
- Спор решает проджект; сокращение объёма — только сэр или продукт.

## Инциденты

- **Нумерация уехала.** Тела задач готовились с расчётом на #7–#13, но параллельная
  сессия по issue #6 заняла #7 (проверочная issue) и #8 (PR с фиксом). Задачи легли
  в #9–#15; все внутренние ссылки исправлены через `gh issue edit`, проверено
  grep-ом по телам. Урок: номер issue не бронируется — ссылки проставлять после
  создания.
- **Общее рабочее дерево — два столкновения за час.** Сначала в дереве появились
  незакоммиченные правки продуктовой сессии (PRD, шаблон, handoff); в коммит этой
  сессии взят только `sessions/project.md`. Затем хуже: пока продукт делал
  `git commit`, эта сессия переключила HEAD — чужой коммит `b7c5e0c` лёг на
  локальный `main` в обход pull request и оттуда попал в основание ветки
  `chore/project-journal`. Продукт переставил свою ветку и вернул `main`
  к `origin/main`; после слияния его PR #17 это тот же объект в `main`,
  и PR #16 конфликта не даёт. Вывод, внесённый в процесс: **дерево не делится —
  каждой роли свой worktree**; сегодня разошлись мирно только потому, что работали
  по очереди и предупреждали друг друга письмами.

## Ревью доски продуктом

Карина сверила задачи #9–#15 с требованиями: замечаний по содержанию нет,
критерии A1–A13 распределены без потерь. Два принятых дополнения:

- **Восьмая задача — сборка сдачи (#18)**, по «Сессии 8» утверждённого плана:
  корневой README с проверкой на чистом клоне, `REPORT.md` включая несработавшее,
  PDF, полнота `sessions/`, финальный прогон `scan_secrets`. Заведена заранее,
  чтобы отчёт писался по журналам, а не по памяти; метка `role:release` создана.
- **Приёмка #9**: версия redocly-линтера фиксируется, `@latest` убран; в CI
  сетевой `npx` не тащится — контракт в CI проверяется валидацией ответов
  в тестах бэкенда (A7).

## Инцидент: прямой push в main — моя ошибка и дыра в хуке

После слияния PR #19 соседняя сессия вернула HEAD общего дерева на `main`.
Я перед коммитом ветку **не проверил** — и коммит `6709eda` (обновление этого
журнала) ушёл голым `git push` прямо в `origin/main`, минуя pull request.
`guard-git` голый push без имени ветки не распознал: правило «в main только
через PR» держалось на регулярке, которую upstream-push обходит. Заведена
bug-задача #20 (метка `role:devops`) с воспроизведением и списком проверок.

Содержимое коммита легитимно — журнал и так шёл в `main`, только через PR;
нарушен процесс, не результат. Рекомендация сэру: не ревертить, историю
не мусорить — механизм чинится в #20. Правило себе с этой минуты: перед любым
коммитом — `git branch --show-current`; push — только адресный, с именем ветки.

Продукт воспроизвёл независимо и расширил находку: под текстовое правило не
попадает не только голый push, но и `git push origin HEAD`, `--all`, `-u origin @`
— любая форма без буквального имени ветки. Суть починки, внесённая в #20: хук
должен смотреть на состояние репозитория (`git rev-parse --abbrev-ref HEAD`),
а не на текст команды; перечислением текстовые формы не закрываются — за день их
обошли одиннадцать раз в обе стороны. **#20 повышена до блокера перед #11 и #12**:
пока дыра открыта, три параллельных роли в общем дереве повторят инцидент уже
с кодом. Примечание внесено в тела #11 и #12.

Отдельно, к сведению: продукт отметил сам способ доклада — инцидент назван вслух
до того, как его кто-то нашёл, воспроизведён и заведён задачей. В критериях курса
честность про несработавшее стоит отдельной строкой; скрытый коммит без PR стоил бы
вечера 28-го на выяснение, чей он. Записываю не ради похвалы, а как рабочий вывод:
называть отрицательный результат раньше, чем его найдут, — часть процесса.

## Правка путей worktree — после слияния PR #19

Пути `../psp-*` из первоначальных задач оказались невыполнимыми: `git worktree add`
наружу отклоняет `guard-scope`, и у этой границы нет пропусков по замыслу — открытая
зона `git-worktree` снимает запрет `guard-git`, но не границу файловой системы.
Решение сэра (внедрено продуктовой сессией, PR #19): **копии живут внутри
репозитория, в `.worktrees/`** — папка в `.gitignore`, сканер недоверенного её
пропускает, три регрессионных теста. Я заменил пути в #9–#13 на `.worktrees/psp-*`.

Отклонённая альтернатива — исключение в `guard-allow.txt` на запись наружу —
отклонена сознательно: пробоина в главной границе проекта ради удобной раскладки
папок обходится дороже, чем три полных копии внутри рабочей директории. Ролям
её не предлагать.

Попутная находка в коллекцию ложных тревог `docs/HOOKS.md`: `guard-scope`
заблокировал команду, в которой `../psp` встречался лишь как **текст паттерна
grep-проверки**, — хук читает упоминание пути в команде как намерение записи.
Обошлось переформулировкой проверки, но случай стоит иметь в виду.

## Контракт принят — и три следствия

`openapi/openapi.yaml` слит (PR #22), #9 закрыта. Приёмка сделана мной задним
числом: 23 структурные проверки против требований — эндпоинты, заголовки, TTL,
четыре статуса, `status_reason`, правило `% 100` с оговоркой про суммы `1` и `2`,
все восемь кодов ошибок, порт 8080. Ненайденного нет, **принято**.

1. **Приёмка опоздала, и это дефект механизма, а не расторопности.** Строка
   `Closes #9` в PR закрыла задачу слиянием — раньше человека. Там, где приёмка
   предусмотрена явно, `Closes` ставить не следует: задача закрывается вручную
   после проверки. Иначе «принимает не автор» держится на очерёдности, а не на
   механизме — то есть является пожеланием. Замечание оставлено в #9.
2. **Контракт разошёлся с PRD.** Валидация тела свёрнута в единый
   `validation_failed` (422) вместо `invalid_amount` / `invalid_currency` (400)
   из F8/F9 — решение сэра, разобрано в #9. Расхождение унаследовали бы четыре
   роли: примечание с точной редакцией внесено в #10, #11, #12, #13, #15;
   продукт правит PRD.
3. **Перекрёстное ревью контракта не состоялось** — сессий бэка и фронта на момент
   слияния не было. Требование не отменено, а перенесено: разбор контракта
   по пятипунктовой форме — первое действие обеих сессий, до кода; найденное идёт
   отдельной задачей на изменение контракта, а не обходится в реализации.

## #24: как я завёл дыру, которой нет

Сессия контракта сообщила: изнутри `.worktrees/psp-contract` набор тестов падает
на проверке «рабочая копия за пределами репозитория отклоняется». Воспроизвёл сам,
дважды: из корня — «ВСЁ ЗЕЛЁНОЕ», из копии — «ПРОВАЛОВ: 1». И завёл задачу
как дыру в безопасности.

**Вывод был неверный.** Разбор кода: `guard-scope` разворачивает относительные пути
от корня проекта, а не от текущей папки (`resolve(candidate, root)`), а корень —
`CLAUDE_PROJECT_DIR` либо cwd (`_hooklib.project_dir()`). У сессии, запущенной
внутри рабочей копии, корень — сама копия. Тест же проверяет команду с жёстко
зашитым `git worktree add ../psp-contract`: из `/repo` это путь наружу — отказ;
из `/repo/.worktrees/psp-contract` тот же текст разворачивается в саму копию —
разрешение. **Совпало имя в тесте с именем рабочей копии.** Из
`.worktrees/psp-design` тот же набор зелёный — проверено.

Граница при этом не ослабляется, а сужается: из такой сессии отказ получают
и пути наружу, и запись в основную копию, и в соседние копии. Проверено прямым
вызовом хука.

Поправка опубликована комментарием в #24 в тот же час: задача переименована
в «набор проверок даёт ложный провал при запуске из рабочей копии», метка `bug`
снята, из блокеров #11/#12 убрана. По делу осталось: развязать тест с именем копии,
добавить прогон изнутри копии, описать поведение корня в `docs/HOOKS.md`.

Разбор подтверждён независимо: сессия контракта проверила его по коду
(`scripts/test_hooks.py:99` и `guard-scope.py:46`), согласилась и сняла свою
первоначальную формулировку про «может создать worktree вне репозитория».
Две сессии сошлись на одном объяснении, каждая читая код сама.

**Урок, за который отвечаю я.** Воспроизведение показывает, *что* сломалось,
но не *почему*; вывод, сделанный между этими двумя вопросами, стоит немного.
Ложная тревога, названная дырой, тратит чужую сессию — та же цена, что у дыры,
названной ложной тревогой, только платят другие.

## #20 закрыта, приёмка сделана

Devops починил: `guard-git` теперь решает по состоянию репозитория, а не по тексту
команды. Проверил не автор и не только тестами — живьём, безопасной формой:
при HEAD на `main` команда `git push --dry-run` получила отказ с точной
формулировкой «push без явного имени ветки при HEAD рабочего дерева на защищённой
ветке «main»». Ровно тот случай, на котором обвязка меня утром пропустила.
Принято, комментарий в #20.

## Макет принят, три роли распараллелены

`design/` слит (PR #27), #10 закрыта. Приёмка сделана мной — третий раз задним
числом, снова из-за строки `Closes` в PR. Сверку вёл не по карте автора, а grep-ом
по `sandbox.html`: пять кнопок сценариев, два мерчанта (`demo-shop-a` / `demo-shop-b`),
пометки «новый платёж» и «тот же платёж», `&lt;script` выведен буквально, коды
`validation_failed` / `idempotency_key_reuse` / `request_in_progress` /
`payment_not_cancelable`, `status_reason`, 104 токена в `tokens.css`, четыре
скриншота на 1280 и 375. Принято без замечаний. Здесь, в отличие от #9,
перекрёстное ревью состоялось до слияния — фронт отревьюил макет, оба пункта
закрыты коммитом `75c2f05`.

**Решение о параллельности.** Раздал работу трём ролям сразу, не выстраивая
их в очередь:

| Роль | Почему стартует сейчас |
|---|---|
| бэкенд #11 | критический путь, зависел только от контракта |
| документация #13 | зависит только от контракта; живой API нужен ей на последнем шаге, для прогона `curl` — держать её за бэкендом значило потерять сутки |
| фронт #12 | контракт и макет закрыты; **бэкенда не ждёт** — контракт для того и писался первым, чтобы стороны строились от него одновременно |

Условие, которое не смягчается: живой бэкенд нужен не для написания кода,
а для приёмки. Скриншоты фронта и `curl`-примеры доков снимаются с работающего
API, моки в приёмку не идут. Обеим сторонам сказано отдельно: увидев в контракте
два прочтения, не подгонять под своё, а писать в #9 — разойтись они могут
только там.

## #24: автор задачи оказался прав, а я — нет во второй раз

Лёша закрыл #24 (PR #30) раньше, чем дошло моё письмо со снятой формулировкой.
Разошлись мы не в диагностике, а в выводе, и его вывод лучше.

Я предлагал **задокументировать** наблюдаемое поведение: «для сессии в рабочей
копии границей становится копия». Он сделал границу **общей** для основной копии
и всех worktree через `git rev-parse --git-common-dir`. Разница не в формулировке:
после его правки моя документация стала бы просто неверной.

Проверил не тестами автора, а прямым вопросом к коду — с намеренно подменённым
`CLAUDE_PROJECT_DIR` на рабочую копию:

```
project_dir(): …/.worktrees/psp-devops
repo_root() :  …/ai-lessons/2      →  совпадают? False
```

Граница определяется репозиторием, а не тем, как запустили сессию. **Принято.**

Главное здесь — не мой промах, а то, чего я не сделал бы сам. Мой вывод
«эксплуатируемой бреши нет» был верным по факту, но опирался на то, что
`CLAUDE_PROJECT_DIR` не меняется при `cd` в копию, — свойство обвязки, не свойство
репозитория. Автор задачи не остановился на «работает» и заменил допущение фактом.
Это разница между «сейчас не сломано» и «сломаться не может».

## Перекрёстное ревью окупилось: шесть пробелов до кода

Бэкенд разобрал контракт по 718 строкам и нашёл пять пробелов (A–E), фронт
подтвердил их своими основаниями и добавил шестой (F, CORS). Ни один не блокирует
реализацию; каждый — место, где две независимые роли написали бы разный код
и обе были бы правы:

| | Пробел | Чем опасен |
|---|---|---|
| A | нечитаемое тело запроса не описано | **любой** ответ на битый JSON нарушает контракт: код вне перечня или 422 вопреки его определению |
| B | у `Idempotency-Key` есть границы, нет кода на их нарушение | асимметрия с мерчантом; достижимо руками из песочницы |
| C | «то же тело» не определено, лишние поля разрешены | идемпотентность рискует стать свойством сериализатора клиента, а не смысла запроса |
| D | порядок проверок между слоями не задан | три законных ответа на один запрос; роли разъедутся на сценариях U4 |
| E | ни одного `5xx` в файле | A7 требует проверки соответствия, а сбой отвечает несуществующим |
| F | CORS нигде не гарантирован | в браузере отказ неотличим от обрыва сети; заголовки нужны и на ошибочных ответах |

Заведены **одной** задачей #32, а не шестью: пробелы связаны, и дёргать
контрактную сессию по одному — три лишних круга. Два условия вписал в задачу
жёстко: отказ от правки законен, но **письменный** («5xx вне контракта» — решение,
если сказано вслух, и умолчание, если нет); и — поскольку бэкенд с фронтом уже
пишут код на временных решениях — каждый час промедления это код под допущение.

Сверх найденного добавил: зафиксировать сужение по гонке. Контракт допускает
у проигравшего и 200, и 409, задача #11 сузила до 409, фронт на это рассчитывает.
Пусть будет решением контракта, а не совпадением двух задач.

## Собственное противоречие в шаблоне

Фронт заметил то, что я проглядел: правило «не ставить `Closes`» я вписал
в комментарии и сигналы, а в **теле** каждой задачи остался чекбокс «PR закрывает
эту задачу строкой `Closes #N`». Тело переживает любую переписку, и следующая роль
прочитала бы его, а не мои письма. Исправлено во всех шести открытых задачах.

Урок ровно тот, вокруг которого построен весь репозиторий: новое правило,
добавленное поверх старого, не отменяет старое. Отменяет — правка того места,
где старое записано.

## Ошибка в собственном критерии приёмки

Документация поймала на разборе задания: `docker build docs-site/` физически
не соберёт страницу — `openapi.yaml` лежит вне этого контекста. Критерий писался
мной по инерции, «как у двух других частей». Исправлен на
`docker build -f docs-site/Dockerfile .`; требование `context: .` передано в #14
отдельным комментарием, туда же — просьба проверить то же самое у фронта.

Шаг «приёмка задания исполнителем» ввели, чтобы ловить непонятое исполнителем.
Ловит он в том числе неверное, написанное постановщиком, — и это не побочный
эффект, а вторая половина его пользы.

## Решения проджекта по ходу

| Вопрос | Решение | Почему |
|---|---|---|
| токены дизайна на странице документации | применить палитру и типографику; при сопротивлении Scalar — нейтральный дефолт и строка в приёмку | проверяющий открывает песочницу и доки подряд, две палитры читаются как две системы; цена — правка одного файла, но читаемая страница дороже наших цветов |
| хук против `Closes` в PR | **не хук** — CI-проверка либо дисциплина шаблона | оценка devops: единственный способ узнать метку задачи из хука — сетевой вызов к GitHub на каждый `gh pr create`; новый класс зависимости (задержки, лимиты, «отказывать или пропускать при сбое сети») у всех остальных хуков локальных. Плюс слой не тот: PreToolUse ограничивает одну сессию, а проблема кросс-сессионная |

## Состояние на конец сессии

- #6, #9, #10, #20, #24 закрыты; все приняты мной, четыре — задним числом.
- PRD в `main` (PR #17): F7a, F17–F18, U4a, A12–A13.
- **#11, #12, #13 идут параллельно**; #12 ждёт только пропуска на рабочую копию.
- **#32 (поправки к контракту A–F)** — заведена, ждёт контрактную сессию.
  Не блокирует реализацию, но чем дольше открыта, тем больше кода на допущениях.
- #14 (compose) ждёт три части — требования к ней копятся комментариями заранее.
- #15 (QA) после #14, #18 (сдача) последняя.

Четыре приёмки задним числом за день. Правило теперь стоит в телах задач,
а не только в переписке, — но механизма за ним по-прежнему нет, и решение,
делать ли CI-шаг, за сэром.
