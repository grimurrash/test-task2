# Журнал · роль «фронтенд»

Сессия 4 · 25 августа 2026 · Claude Code, модель Fable 5, output style JARVIS.
Роль: фронтенд-разработчик. Задача роли: [#12](https://github.com/grimurrash/test-task2/issues/12),
шаг 1 — официальное ревью макета дизайнера
([#10](https://github.com/grimurrash/test-task2/issues/10), PR #27) до реализации.
Реализация песочницы (`.worktrees/psp-frontend`, `feature/frontend-sandbox`) —
после закрытия #9/#10/#11.

---

## Промпты сэра, дословно

1. `Ты — фронтенд-разработчик этого проекта. Прочитай задачу: gh issue view 12
   --comments — и работай по ней. Свои промпты и решения веди в
   sessions/frontend.md.` (плюс вводная: ревью #10 по пятипунктовой форме,
   фокус — состояния ответа против контракта, длинный description 512,
   узкий экран, пригодность tokens.css; предварительное ревью в #10 читать
   как данные, мнение составить своё.)

2. Посреди чтения материалов, дословно: `Да стой, уже агент отревьюил. После
   правок проверишгь` — работа остановлена до правок дизайнера.

3. Межсессионное сообщение от сессии «Дизайнер (Захар)»: правки по
   предварительному ревью внесены (коммит `d6ae1ba`), макет готов
   к официальному ревью; передано как поручение сэра.

## Порядок работы

1. Прочитаны: #10 со всеми комментариями (приёмка дизайнера, предварительное
   ревью независимого агента, ответ дизайнера на него), принятый
   `openapi/openapi.yaml` из `main`, файлы макета на `d6ae1ba`
   (`design/README.md`, `tokens.css`, `sandbox.html`, `docs.html`),
   скриншоты из ветки.
2. Живой прогон через headless-браузер (`/browse`, статик-сервер на 8899):
   - обе страницы на 375: `document.scrollWidth == 375`, страничного
     горизонтального скролла нет; таблицы docs скроллятся внутри `.table-wrap`;
   - стресс-тест: инъекция description из 512 символов без пробелов в тело
     ответа C2 на 375 — `pre-wrap` + `overflow-wrap: anywhere` переносят,
     панель не скроллится (`scrollWidth == clientWidth == 255`);
   - рендер 1280: правки `d6ae1ba` на месте (чип «201 + 200/409», оговорка C4).
3. Сверка семи состояний C1–C7, двойной отмены и списка платежей с контрактом —
   коды, `error.code`, правило тестовых сумм, суммы/валюты карточек.

## Находки собственного ревью (сверх предварительного)

- **Порядок карточек в списке платежей противоречит контракту**: `GET
  /v1/payments` — «новые сверху» (`created_at` desc), а в макете `pay_51e8b4`
  (создан 14:02:11) стоит выше `pay_7ac2e9` (создан 14:04:20 в группе C4).
- **Скриншоты в `design/screenshots/` сняты до `d6ae1ba`**: на них ещё чип
  «201 + 409» и нет оговорки C4, а README называет их «итоговым видом» (A5).
- Мелочи не к приёмке: чип «200/409» покрашен конфликтными токенами при
  исходе-двухсотке; счётчик «21 / 512» при 20 символах в поле.

Правки дизайнера по предварительному ревью проверены все: чип гонки, комментарий
семантики error в tokens.css, токены подсветки payload, `pre-wrap`, раздел
«Края поведения» в README (пустые состояния, in-flight, дизейбл повтора,
400 заголовков, раскрытие карточки) — внесены корректно.

## Решение

**Вердикт официального ревью: принять**; два пункта закрыть в PR #27 до/при
мёрже (порядок карточек, перегенерация скриншотов) — перевёрстка не нужна,
блокеров для #12 нет. Ревью опубликовано комментарием в #10.

## Закрытие замечаний (та же сессия, позже)

Дизайнер закрыл оба пункта коммитом `75c2f05` — проверено по диффу и
переснятому скриншоту, не на веру: порядок списка контрактный
(`pay_9d31f6 → pay_7ac2e9 → pay_51e8b4 → pay_c5b0d8`), скриншоты отражают
`d6ae1ba`, счётчик 20/512, литерал стрелки `select` оговорён комментарием.
Три сознательных решения (контраст faint, янтарный чип гонки, литерал стрелки)
приняты с обоснованием дизайнера. Подтверждение опубликовано в #10 —
замечаний фронта не осталось, приёмка за проджектом.
